move config over to actual json
then parse the json, which becomes the exported variable
https://firebase.google.com/integrations/

name ideas
newlink 

first year is free


//need to change input value props because props are static
//i need to get it into state.

useful npm packgase
dotenv
envify